import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'm1p12mean.andrea.alpha',
  appName: 'm1p12mean-andrea-alpha',
  webDir: 'www'
};

export default config;
